import type { NextComponentType, NextPageContext } from "next";

interface Props {}

const SuggestedAccounts: NextComponentType<NextPageContext, {}, Props> = (
  props: Props,
) => {
  return (
    <div>
        Suggested Follows    </div>
  )
}

export default SuggestedAccounts